# Shared ablation banks

The ablation notebooks create deterministic shared training and validation
banks in this folder. The cache files are intentionally not versioned because
they are regenerable and too large for a regular GitHub repository. All seven
notebooks use the seeds and cache names defined in their configuration cells.
